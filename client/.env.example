# En développement avec `npm run dev` : laisser VITE_API_URL vide ou commenté.
# Les appels passent par le proxy Vite (vite.config.ts) vers http://localhost:8000
# VITE_API_URL=

# Production ou accès direct au backend (sans proxy) :
# VITE_API_URL=http://localhost:8000
